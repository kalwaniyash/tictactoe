import React, { Component } from 'react';
import {View} from 'react-native';
import { database } from './firebase';

const Test = ({otherPersonsUid}) => {
    console.log(otherPersonsUid);
    return <View />;
};

export default Test;